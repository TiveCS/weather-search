import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Home | Weather Search</title>
        <meta name="description" content="Weather location monitor app with search location feature" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <p>asd</p>

    </div>
  );
};

export default Home;
